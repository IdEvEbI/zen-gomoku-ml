---
description: PR 合并后与开始新 Issue 的标准流程，用户说「合并完 PR」时按此执行
alwaysApply: true
---

# Feature 开发流程

当用户表示 **PR 已合并**（如「我合并完 PR 了」「PR 合并完成」）时，按顺序执行：

1. **切到 develop 并拉取**：`git checkout develop` → `git pull origin develop`
2. **确认与已合并的 feature 分支一致后，删除该本地分支**：`git branch -d feature/<刚合并的分支名>`
3. **按 Backlog 开下一个 Issue 分支**：阅读 `docs/project/issue-backlog.md` 确定下一个 Issue，执行 `git checkout -b feature/<新分支名>`（分支名与 Issue 主题对应）

完整说明见 **docs/development/feature-workflow.md**。执行前可简要复述这三步，待用户确认后再执行。

开始新 Issue 开发时：从 Backlog 取当前 Issue 的标题、描述与验收标准；**先对齐规格/设计文档再写代码**（见 `docs/development/spec-driven-development.md`）。实现后跑 `npm run test`（Python unittest）与相关训练冒烟，提交信息含 Issue 编号，PR 的 Base 选 `develop`。

## 提交前质量检查

当用户要求 **commit / 提交** 时：

1. **先跑本地检查**（提交前主动执行，不要只依赖 hook）：
   - 改了 `gomoku_ml/` 或训练脚本：`npm run test`（或针对性 unittest）
   - 改了 Markdown（含 `docs/`、`.cursor/**/*.mdc`）：`npm run format:check`；若失败则 `npm run format` 后把改动一并纳入提交
2. **再执行 git commit**。仓库已配置 husky `pre-commit` → `lint-staged`（Prettier，含 `*.md` / `*.mdc` / `*.json`），格式错误会拦截提交；`commit-msg` 仍走 commitlint。
3. **不要**用 `--no-verify` 跳过 hook，除非用户明确要求。CI 会再跑 `format:check` 与 `unittest`，是最终门禁。
